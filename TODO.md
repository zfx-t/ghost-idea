1. 仍然需要获取思考链才可以更好的进行输出，不然等待时间太差，不符合预期（完成）
2. 优化输出结构，让其更具体，更规整。甚至符合json最优格式（完成）
3. 尝试编写工具，让AI可以捕获一些最新时事热点等数据（不够优秀）
4. 生成词云，可以从这些词云中来对自己想做的进行组合。(可以是不同的技术栈，也可以是一些生活中的一些搞笑观点之类的。)
5. 

3.15记录新问题
因为system_prompt的原因，AI失去了很大的创造力，同时他只会根据输入的一下简单内容去进行网络检索。从而检索的内容每次都是那几个。也会导致数据的固定。

### 解决思路：
1. 优化system_prompt，使其更加开放，可以针对其中一些固定的句子，改变成可以插入词云的一些关键词。使其变成一个template模板
2. 使用一些其他的API，主动获取最新的新闻或者一些热点话题，然后插入到system_prompt中，使其更加动态。
3. 可以先不使用网页搜索等功能，仍然需要改善。

词云也可以从一些其他地方获取。